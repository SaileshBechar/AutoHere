import React from 'react'
import { View, Text } from 'react-native';

interface ListTripsProps {

}

export const ListTrips: React.FC<ListTripsProps> = ({}) => {
        return (
            <View>
                <Text>Wow! So many trips!</Text>
            </View>
        );
}